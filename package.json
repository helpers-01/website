{
  "name": "helpers-app",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "dev": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "dev:all": "concurrently \"npm run dev\" \"npm run dev:server\"",
    "build": "npm run build --workspace client && npm run build --workspace server",
    "build:client": "npm run build --workspace client",
    "build:server": "npm run build --workspace server",
    "start": "npm run start --workspace client",
    "start:server": "npm run start --workspace server",
    "start:prod": "concurrently \"npm run start\" \"npm run start:server\"",
    "test": "npm run test --workspace client && npm run test --workspace server",
    "test:client": "npm run test --workspace client",
    "test:server": "npm run test --workspace server",
    "lint": "npm run lint --workspace client && npm run lint --workspace server",
    "lint:fix": "npm run lint:fix --workspace client && npm run lint:fix --workspace server",
    "type-check": "npm run type-check --workspace client && tsc --noEmit --project server/tsconfig.json",
    "db:migrate": "node scripts/migrate.js",
    "db:seed": "node scripts/seed.js",
    "deploy": "node scripts/deploy.js",
    "deploy:frontend": "node scripts/deploy.js frontend",
    "deploy:backend": "node scripts/deploy.js backend",
    "docker:up": "docker-compose up",
    "docker:down": "docker-compose down",
    "docker:build": "docker-compose build",
    "docker:logs": "docker-compose logs -f",
    "monitoring:up": "docker-compose --profile monitoring up",
    "security:audit": "npm audit --audit-level=high",
    "clean": "npm run clean --workspace client && npm run clean --workspace server && rm -rf node_modules"
  }
}