{
  "watch": ["src", "../shared"],
  "ext": "ts,js,json",
  "ignore": ["src/**/*.spec.ts", "src/**/*.test.ts", "dist/**/*"],
  "exec": "ts-node -r tsconfig-paths/register",
  "env": {
    "NODE_ENV": "development"
  }
}
